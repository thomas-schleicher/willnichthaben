import { Component } from '@angular/core';

@Component({
  selector: 'app-listings',
  imports: [],
  templateUrl: './listings.component.html',
  styleUrl: './listings.component.scss'
})
export class ListingsComponent {

  // let user choose if its vehicle, property or retail
  // fetch the corresponding interface
  // generate form based on interface, and what user is allowed to input
  // cross reference interfaces in backend to ensure data is valid
}
